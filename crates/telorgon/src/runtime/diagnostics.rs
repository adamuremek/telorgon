#[derive(Copy, Clone, Debug, Default, PartialEq, Eq)]
pub struct ComponentDiagnostics {
    pub components_created: u64,
    pub components_mounted: u64,
    pub components_unmounted: u64,
    pub live_components: usize,
    pub live_states: usize,
    pub live_reads: usize,
    pub live_read_dependencies: usize,
    pub live_bindings: usize,
    pub live_observers: usize,
    pub live_input_routes: usize,
    pub live_structural_containers: usize,
    pub live_tasks: usize,
    pub live_timers: usize,
    pub transactions: u64,
    pub rejected_transactions: u64,
    pub state_writes_staged: u64,
    pub state_writes_coalesced: u64,
    pub state_writes_committed: u64,
    pub equal_writes_suppressed: u64,
    pub bindings_patched: u64,
    pub actions_handled: u64,
    pub stale_actions: u64,
    pub reads_evaluated: u64,
    pub unchanged_reads: u64,
    pub read_cycles: u64,
    pub observer_actions: u64,
    pub action_round_limit_hits: u64,
    pub tasks_started: u64,
    pub tasks_completed: u64,
    pub task_results_delivered: u64,
    pub tasks_cancelled: u64,
    pub task_host_shutdowns: u64,
    pub task_starts_failed: u64,
    pub stale_task_results: u64,
    pub task_queue_high_water: usize,
    pub timers_started: u64,
    pub timers_fired: u64,
    pub timer_actions_delivered: u64,
    pub timers_cancelled: u64,
    pub stale_timer_deadlines: u64,
    pub missed_timer_intervals: u64,
    pub timer_queue_high_water: usize,
    pub structural_containers_visited: u64,
    pub structural_inserted: u64,
    pub structural_removed: u64,
    pub structural_moved: u64,
    pub structural_reused: u64,
    pub structural_replaced: u64,
}
